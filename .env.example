# Copy to .env and set your cache server address once.
LANCACHE_SERVER_IP=192.168.1.10
# Optional custom management origin (for example, a reverse proxy):
# LANCACHE_PUBLIC_ORIGIN=https://cache.example.lan
